query issues($org: String!, $repo: String!) {
  repository(owner: $org, name: $repo) {
    issues(last: 100){
      nodes{
        id
        databaseId
        title
        bodyText
        author {
          login
        }
        number
        milestone {
          title
        }
        state
      }
    }
  }
}
