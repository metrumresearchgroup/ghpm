query issue_comments($org: String!, $repo: String!) {
  repository(owner: $org, name: $repo) {
    issues(last: 100){
      nodes{
        number
        id
        databaseId
        comments(first: 50){
          nodes{
            author {
              login
            }
            createdAt
            body
          }
        }
      }
    }
  }
}
