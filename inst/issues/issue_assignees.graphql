query issue_assignees($org: String!, $repo: String!) {
  repository(owner: $org, name: $repo) {
    issues(last: 100){
      nodes{
        number
        id
        databaseId
        assignees(first:10) {
          nodes {
            login
          }
        }
      }
    }
  }
}
