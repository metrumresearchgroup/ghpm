query issue_comments($org: String!, $repo: String!) {
  repository(owner: $org, name: $repo) {
    issues(first: 100){
      nodes{
        number
        labels(first: 15) {
          nodes {
            name
          }
        }
      }
    }
  }
}
