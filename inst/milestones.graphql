query milestones($org: String!, $repo: String!) {
  repository(owner: $org, name: $repo) {
    milestones(first: 100) {
      nodes {
        title
        description
        creator {
          login
        }
        state
        url
      }
    }
  }
}
