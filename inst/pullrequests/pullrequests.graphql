query pullrequest($org: String!, $repo: String!) {
  repository(owner: $org, name: $repo) {
    pullRequests(first: 100) {
      nodes {
        number
        title
        author {
          login
        }
        bodyText
        milestone {
          title
        }
        createdAt
        mergedBy {
          login
        }
        mergedAt
        baseRefName
        state
      }
    }
  }
}
